#include <iostream>

using namespace std;

int main () {

    for (int index = 1, index2 = 10 ; index < 11 ; index++, index2-- ) {
        cout << index << "\t" << index2 << endl;
    }

    return 0;
}